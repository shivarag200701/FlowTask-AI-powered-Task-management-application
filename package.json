{
  "name": "flow-task",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "frontend",
    "backend",
    "common"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "turbo": "^2"
  }
}
